language: python
python:
  - "2.7.15"

# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
#script:
 # - tests
  
script:
  - python -m unittest -v devcolbj/odoo/addons/tests
